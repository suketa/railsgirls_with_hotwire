<%= turbo_stream.append "comments" do %>
  <%= render "comments/comment", comment: @comment, idea: @idea %>
<% end %>
<%= turbo_stream.replace "new_comment", partial: "comments/form", locals: { idea: @idea, comment: Comment.new } %>
